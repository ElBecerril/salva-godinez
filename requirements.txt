rich==14.3.2
pypdf==6.6.0
openpyxl==3.1.5
Pillow==12.1.0
# CA bundle para verificar HTTPS del auto-updater desde el .exe congelado:
# el ssl de Windows/PyInstaller no encuentra los CA del sistema y la conexion
# a api.github.com fallaba en silencio. Ver tools/updater._get_ssl_context.
certifi==2026.7.22
# PyMuPDF==1.25.3  # Opcional: solo para "PDF a Imagenes" (pip install PyMuPDF)
